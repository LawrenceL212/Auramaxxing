# Blender source files stay out of the repo - large binaries the runtime never
# consumes. Only exported assets under 3d/exports/ ship.
*.blend
*.blend1

# Local remnants of the removed 3D Hunter pipeline (preserved in git history
# on the pre-reset branches; not part of the live app).
exports/hunter-*
exports/_regions-*.npz
source/hunter/

# Python bytecode
__pycache__/
*.pyc

# Local QA captures
qa-reset/
qa/
